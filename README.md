# hue-browser-controller_docker-compose
docker-compose file and Dockerfile for hue-browser-controller (https://github.com/JakobJFL/Hue-Browser-Controller)

Container uses nginx:alpine image.

When inside folder:

```
docker-compose up -d
```
